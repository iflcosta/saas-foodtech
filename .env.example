# apps/api — backend Fastify
DATABASE_URL=postgres://postgres:postgres@localhost:5432/saas_foodtech
JWT_SECRET=change-me
ASAAS_API_BASE=https://sandbox.asaas.com/api/v3
ASAAS_API_KEY=
PORT=3000
HOST=0.0.0.0

# apps/pos — PWA do PDV
VITE_API_BASE_URL=http://localhost:3000

# apps/bridge — ponte de impressão local (Go)
BRIDGE_PORT=9100
