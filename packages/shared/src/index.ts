/**
 * Ponto de entrada de `@saas-foodtech/shared`.
 *
 * Por enquanto, o pacote expõe somente o namespace de validação Zod
 * (`./validation`). Novos namespaces (constantes, types compartilhados)
 * devem ser adicionados aqui à medida que surgirem.
 */
export * as validation from './validation/index.js';
export * from './validation/index.js';
